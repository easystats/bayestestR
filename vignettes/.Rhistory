df <- read.csv("https://raw.github.com/DominiqueMakowski/bayestestR/master/data/bayes_indices.csv")
x <- t.test(df$beta, mu = 1)
x$parameter
x$estimate
hdi(df$beta, prob=0.95)
library(bayestestR)
hdi(df$beta, prob=0.95)
round(hdi(df$beta, prob=0.95), 2)
paste0("Median = ", round(median(df$beta), 2), "[", paste(round(hdi(df$beta, prob=0.95), 2), collapse = ", "))
paste0("Median = ",
round(median(df$beta), 2),
" 95% CI [",
paste(round(hdi(df$beta, prob=0.95), 2), collapse = ", "),
"]")
paste0("Median = ",
round(median(df$beta), 3),
", 95% CI [",
paste(round(hdi(df$beta, prob=0.95), 2), collapse = ", "),
"]")
median(df$beta)
paste0("Median = ",
round(median(df$beta), 4),
", 95% CI [",
paste(round(hdi(df$beta, prob=0.95), 3), collapse = ", "),
"]")
paste0("Beta: Median = ",
round(median(df$beta), 4),
", 95% CI [",
paste(round(hdi(df$beta, prob=0.95), 4), collapse = ", "),
"]")
paste0("Beta: median = ",
round(median(df$beta), 4),
", 95% CI [",
paste(round(hdi(df$beta, prob=0.95), 4), collapse = ", "),
"]")
signif(median(df$mean), 4)
round(median(df$mean), 4)
format(median(df$mean), 4)
median(df$mean)
format(median(df$mean), digits=4)
format(median(df$mean), nsmall=4)
format(median(df$mean), nsmall=3)
median(df$mean)
format(median(df$mean), digits=3, nsmall=3)
format(median(df$mean), digits=4, nsmall=3)
format(median(df$mean), digits=4, nsmall=4)
median(df$mean)
round(median(df$mean), nsmall=4)
round(median(df$mean), 4)
prettyNum(median(df$mean), 4)
prettyNum(median(df$mean), digits=4)
prettyNum(median(df$mean), digits=4, format = "fg")
prettyNum(median(df$mean), digits=4, format = "f")
prettyNum(median(df$mean), digits=4, format = "g")
prettyNum(median(df$mean), digits=4, drop0trailing=FALSE)
formatC(median(df$mean), digits=4, drop0trailing=FALSE)
formatC(median(df$mean), digits=4)
round(median(df$mean), 4)
round(median(df$mean), 5)
df$sig_05 <- factor(ifelse(df$p >= .1, "n.s.", "°"), levels=c("n.s.", "°"))
df$sig_05 <- factor(ifelse(df$p >= .1, "n.s.", "°"), levels=c("n.s.", "°"))
df$sig_1 <- factor(ifelse(df$p >= .1, "n.s.", "°"), levels=c("n.s.", "°"))
data <- rbind(
get_data(predictor="bayes_p", outcome="sig_1", lbound=0, ubound=0.5),
get_data(predictor="pd", outcome="sig_1", lbound=90, ubound=100),
get_data(predictor="rope", outcome="sig_1", lbound=0, ubound=0.1),
get_data(predictor="rope_overlap", outcome="sig_1", lbound=0, ubound=0.1))
data %>%
mutate(index = as.factor(index)) %>%
ggplot(aes(x=value, y=sig_1, color=index)) +
geom_line(size=1) +
facet_wrap(~index, scales = "free") +
theme_classic() +
theme(strip.background = element_blank()) +
scale_color_manual(values = c("bayes_p" = "#f44336", "pd" = "#2196F3", "rope" = "#4CAF50", "rope_overlap" = "#FFC107")) +
ylab("Probability of being significant at p < .1\n")
knit_with_parameters('C:/Users/Dom/Dropbox/RECHERCHE/N/bayestestR/vignettes/readme.Rmd', encoding = 'UTF-8')
rope_overlap(rnorm_perfect(1000, 0, 1)
)
rope_overlap(rnorm_perfect(1000, 2, 0.01))
rope_overlap(rnorm_perfect(1000, 0, 0.001))
rope_overlap(rnorm_perfect(1000, 0, 1))
x = rnorm(1000, 0, 0.1)
